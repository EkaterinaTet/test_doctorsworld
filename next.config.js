/** @type {import('next').NextConfig} */

// const isProd = process.env.NODE_ENV === "production";

const nextConfig = {
  // output: "export",
  // basePath: isProd ? "/test_doctorsworld" : "",
  // assetPrefix: isProd ? "/test_doctorsworld/" : "",
};

module.exports = nextConfig;
